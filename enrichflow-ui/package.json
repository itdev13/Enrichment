{
  "name": "enrichflow-ui",
  "version": "0.1.0",
  "description": "EnrichFlow Custom Page UI (embedded in GoHighLevel) — connect status, single + bulk enrich, usage dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
